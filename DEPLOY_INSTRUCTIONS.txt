PASSO A PASSO FINAL (VPS):

1. ATUALIZE O NGINX:
   sudo nano /etc/nginx/sites-available/gestaokavins.com.br
   (Cole o conteúdo do NGINX_CONFIG.conf atualizado)

2. TESTE E REINICIE O NGINX:
   sudo nginx -t
   sudo systemctl restart nginx

3. NO PROJETO CORTE (/var/www/gestaokavins/corte):
   npm run build
   pm2 restart kavins-corte || pm2 start server.js --name "kavins-corte"

4. NO PROJETO PRINCIPAL (/var/www/gestaokavins/pedido):
   pm2 restart kavins-principal

LIMPEZA DE CACHE:
Se ao acessar gestaokavins.com.br/corte ainda aparecer o projeto principal, 
aperte CTRL + F5 no navegador para limpar o cache de rota.